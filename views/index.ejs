<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/main.css" />
    <!-- <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css"/> -->
    <title>Document</title>
</head>
<body>
    <section>
        <div>
            <h2 class="title">Confessions</h2>
        </div>
    </section>
    <main class="container">
        <div>
            <div>
               <h1>This is our secret index</h1>
               <a href="/secrets/new/<%=secrets._id%>"> Add Your Secret</a>
               <% for(let idx=0; idx < secrets.length; idx++){ %>
                <% const secret = secrets[idx]  %>
                <div class="secret">
                <h1>category: <%=secrets[idx].category%></h1>
                <h1>secret: <%=secrets[idx].content%> </h1>
                <h1><%=secret.username.username%></h1>
                </div>
                <div class = "edit">
                <a href="/secrets/<%= secrets[idx]._id %>/edit">Edit</a>
                <a href="/secrets/<%=secrets[idx]._id%>">Comment</a>
                </div>
                <div class="delete">
                    <form method="POST" action="/secrets/<%= secret._id%>?_method=DELETE">  
                        <input type="submit" value="Delete Secret"/>
                    </form>
                </div>
                <div>
               
                </div>
    
                <% } %>
            </div>
        </div>
    </main>
</body>
</html>